includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - src/

    # Level 9 is the highest level
    level: 5

    ignoreErrors:
        # Ignore common false positives or known issues if any

    excludePaths:
        - ./*/*/FileToBeExcluded.php
